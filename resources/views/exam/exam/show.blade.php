@extends('student.layout-copy')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Exam {{ $exam->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/exam/exam') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/exam/exam/' . $exam->id . '/edit') }}" title="Edit Exam"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('exam/exam' . '/' . $exam->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Exam" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $exam->id }}</td>
                                    </tr>
                                    <tr><th> Name </th><td> {{ $exam->name }} </td></tr><tr><th> Dekskripsi </th><td> {{ $exam->dekskripsi }} </td></tr><tr><th> Topic Id </th><td> {{ $exam->topic_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
